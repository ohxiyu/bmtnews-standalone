# 07:26 日报与 AI 节流（Issue90）

## 范围与来源

用户确认 Asia/Shanghai 07:00 截止、07:26 启动；不是保证07:26完成发布。原生产实际08:30启动，故按明确时间前移64分钟，不沿用口头“半小时”。主Cron对应前一UTC日23:26。余额不足由用户反馈，未访问账单或充值。

[DeepSeek官方公告](https://api-docs.deepseek.com/news/news260910/)规定V4.1 Flash的API名为deepseek-flash；旧deepseek-v4-flash只是临时映射。本次使用正式名称，不伪造deepseek-v4.1-flash。按[官方错误码](https://api-docs.deepseek.com/quick_start/error_codes/)，402是余额不足。

## 实现

- 日报/CLI默认7点截止，Worker07:26启动和08:11逾期，共享恢复锁/冷却/预算不变；Watchdog07:43、08:17–15:17；采集备用入口07:37，其余原四小时采集不减，避免高周转来源漏收。
- Worker仍只有三组Cron；共享回调从08:00起，但币安广场09:00–23:30窗口不变。保留旧Cron路由，不重置Durable Object。
- 首页中英文文案准确标明“开始生成”；08:11才显示延迟。不修改字体、布局或信息流两期规则。
- 去重小组上限由12变24，正常14条榜单从三组重叠比较（28份条目输入）改为一次（14份）；大组采用12项块两两比较，仍覆盖每对新闻。标题/摘要输入上限不缩短，1536输出上限及非思考模式不变；超限/无效响应仍拒绝发布。
- 只缓存通过结构校验的去重结果，命中再次校验；键包含模型、策略指纹、完整提示词和有序实际输入（标题、发布时间、标签、摘要）。TTL和4000条总上限沿用现有缓存，不新增服务。单次比较次数减少不等于全链token减少同样比例，真实判定质量/费用尚待上线观测。
- 工作流改用显式restore/save，并always保存缓存，失败重跑复用之前成功的分析；保留staging-cache后备。不缓存失败、截断或格式错误输出。报告增加comparison_cache_hits/misses。
- HTTP402/401/403在单次去重立即失败，不再重复调用；其他错误仍最多两次应用层尝试（SDK自身重试独立）。只暴露白名单原因，不打印模型原文或凭据。未绕过去重、评分门槛、Crypto目标/AI与政策上限或X发送身份保护。

模型键切换有一次冷缓存成本，不能把旧模型结果当成新模型分析。未为了省token削减来源或候选规模。已有复杂事件推理模式维持原样。

## 验证与上线边界

最终验证：fetch/merge main；uv sync --frozen --extra dev；809 pytest通过（1既有google-genai弃用warning）；48 Worker测试、56 Node测试；Worker生成类型检查、tsc与部署dry-run；治理和diff检查通过。新测试覆盖跨UTC日07:26、毫秒级提前拒绝、08:11逾期、旧窗口显式重刊、一小时重叠、14项单次及62项全对覆盖、缓存重启/失效/损坏与永久错误不重试。

无真实付费模型调用、充值、生产修改或重刊。测试用模拟模型，只证明比较覆盖/缓存失效/错误保护，不证明所有语义判断正确。首页仅文案和时间条件变化，行为测试通过，未做浏览器视觉截图验收。

Worker health核对仍为341dd1ba-4355-42c9-93dc-4133295b53e7、08:30；PR87虽已合并但两次重刊失败，公开日报仍是2026-09-16旧版，前三条重复尚未消除。前次证据见[PR89](https://github.com/ohxiyu/bmtnews-standalone/pull/89)；不得把旧交接覆盖本任务指针。

上线必须单独获授权，并协调main工作流与Worker一起切换，不能只合并等待自动更新Worker。在当前日报完成后、次日07:26前部署并核对真实schedule和cutoff=7。保留Secrets/其他变量和发送记录。初次切换有一小时窗口重叠，历史去重不关；历史08:00刊期重刊必须显式保留旧窗口。部署后再记录实际token、缓存命中、07:26自然触发及日报质量，不提前承诺节省百分比。

依赖安装报告现有Worker devDependencies存在6条审计提示（2moderate/4high）；本任务未升级依赖或执行破坏性audit fix，需独立评估。
