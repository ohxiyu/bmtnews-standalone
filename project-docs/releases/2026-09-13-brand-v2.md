# 圆润品牌 v2 发布记录

交付：[PR #83](https://github.com/ohxiyu/bmtnews-standalone/pull/83)，最终检查与 SHA 以 PR 为准。

任务：[Issue #82](https://github.com/ohxiyu/bmtnews-standalone/issues/82)。用户在本任务明确授权替换 Logo、更新媒体文件夹、合并及上线；Codex / brand-v2 是本次发布整合负责人。基线 `43fb8a0697c4fa0a25d389384179c611cd60593c`，应用版本仍为 0.2.0，独立调度 Worker 不改动。

## 变更范围

按确认的第二版设计稿制作原生 SVG：圆润 b 形主体、两条新闻留白、黄色圆环和墨黑圆心。外凸圆角统一为 64，内凹处 32，两条 176 × 36 留白圆角 12；圆心位置 (380, 284)，半径 52 / 22。母版使用三条路径，生成的网站内嵌图标、媒体 SVG、PNG、PWA 与分享卡共用同一几何。

保留现有已转曲字标及字体许可，不更换运行时字体。黑白版圆环透明、圆心同色；彩色版使用网站蓝、暖白及 #FFCC29。媒体资源包更新为 v2，favicon、Apple、manifest 和默认分享图改用新文件名；旧 v1 图标和 ZIP 不覆盖、不删除，继续服务旧缓存页面。

修改 `brand/`、两个品牌生成脚本、公开媒体资源、图标引用、分享卡三路径绘制、同步资源版本和品牌测试。新资源指纹 `sha256-7e6b0e219d87`。不改变 Quick Post、数据、AI、认证、调度、外部分发或任何云资源绑定。

## 验证与交接

- 依赖：`uv sync --frozen --extra dev --offline` 成功，未改锁文件。
- 构建：使用原有脚本生成 SVG、sharp 导出 PNG / ICO、生成 ZIP。未使用效果图裁切充当正式图标。
- 测试：773 项 Python 测试、37 项 Node 测试通过；治理检查和 git diff --check 通过。首次测试指出指纹计算误含非 CSS/JS 文件，已改为仓库规定的 CSS/JS 范围并完整重跑通过。
- 本地视觉：用实际 gh-pages 历史构建，Jekyll 成功；1280 × 720 桌面媒体页浅/深色资源正常，390 × 844 手机页无横向溢出；分享长卡生成 1320 × 3726，三条母版路径正常显示。仅在本地运行旧 Jekyll / Ruby 4 的命令级兼容处理，未改生产依赖。
- 截图：[替换前桌面](../assets/brand-v2/before-desktop.png)、[新版桌面](../assets/brand-v2/after-desktop.png)、[媒体页](../assets/brand-v2/media-desktop.png)、[手机](../assets/brand-v2/after-mobile.png)、[深色手机](../assets/brand-v2/dark-mobile.png)、[分享长卡](../assets/brand-v2/share-desktop.png)。
- 生产：尚未发布。本地通过、分支预览成功、PR 合并均不能代替主站验收。
- 已安装 PWA 的桌面图标刷新由系统控制；不建议卸载或清空阅读记录。网站更新入口可用于获取新页面资源。
- 其他任务：开放 PR #68、#77、#81 属于其他任务的发布证据，不在本次授权范围内合并；原状态见各自 Issue/PR。

## 发布与回退计划

PR 检查通过后合并，沿用 Deploy Docs → gh-pages → Cloudflare Pages。核验主站图标路径、媒体页下载链接、v2 ZIP、manifest / Apple / social URL 与新资源版本。独立调度 Worker 无需部署，不能顺带触发内容推送。

回退以本任务之前的 `43fb8a0697c4fa0a25d389384179c611cd60593c` 为代码参考，通过 revert 品牌 PR 并走正常发布链路，不回滚内容数据库或其他任务代码，不手工写 gh-pages。回退操作需届时确认权限。
