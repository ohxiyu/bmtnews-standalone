name: BMTNews Schedule Watchdog

on:
  schedule:
    # UTC schedules: 08:47, then hourly 09:17-15:17 in Asia/Shanghai.
    # Repeated checks recover when either scheduler drops or delays one event.
    - cron: '47 0 * * *'
    - cron: '17 1-7 * * *'
  workflow_dispatch:

concurrency:
  group: bmtnews-schedule-watchdog
  cancel-in-progress: false

permissions:
  actions: write
  contents: read

jobs:
  watchdog:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Check daily edition heartbeat
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: >-
          python -m src.schedule_watchdog
          --repository "${GITHUB_REPOSITORY}"
          --workflow daily-summary.yml
          --ref main
          --timezone Asia/Shanghai
          --cutoff-hour 8
          --grace-minutes 47
